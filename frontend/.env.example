# Base URL of the backend API.
# Local development: leave unset — the frontend uses '/api',
# and vite.config.js proxies /api -> http://localhost:4000.
# Production: set to the deployed backend URL, e.g.
# VITE_API_URL=https://backend-two-delta-twnhtuwv4g.vercel.app
VITE_API_URL=